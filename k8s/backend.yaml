---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          # 여러분의 backend image 주소를 입력해주세요.
          image: krmp-d2hub-idock.9rum.cc/dev-test/repo_7f2d4c426e5b
          env:
            - name: TZ
              value: Asia/Seoul
            - name: DATABASE_URL
              value: jdbc:mysql://mysql:3306/honjaya
            - name: DATABASE_USERNAME
              value: root
            - name: DATABASE_PASSWORD
              value: root
            - name: JWT_SECRET
              value: O9HnV8bd+WBo6ZNFKqcKjDPAkCGXb6JWTc7Pv7IUWy4=
            - name: REDIS_HOST
              value: redis
            - name: CLIENT_ID
              value: f80b172c8fd2c4405878f3227740f910
            - name: CLIENT_SECRET
              value: qOvMqL5ksCA74HfLsBkLf6A7LLTWAYcs
            - name: REDIRECT_URI
              value: https://k9b06b863bf82a.user-app.krampoline.com/api/login/oauth2/code/kakao
          resources:
            requests:
              memory: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  selector:
    app: backend
  ports:
    - port: 8080
      targetPort: 8080